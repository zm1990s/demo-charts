apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitlab-light.fullname" . }}
  labels:
{{ include "gitlab-light.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "gitlab-light.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
