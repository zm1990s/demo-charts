apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitlab-light.fullname" . }}-test-connection"
  labels:
{{ include "gitlab-light.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "gitlab-light.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
